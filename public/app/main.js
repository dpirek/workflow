import Router from './router.js';
const app = document.querySelector('#app');
const router = new Router();
let state = { user: null, view: 'overview', workflows: [], instances: [], tasks: [], jobs: [], incidents: [] };
const api = (p, o = {}) => fetch(p, { headers: { 'content-type': 'application/json' }, ...o }).then(async r => { const d = await r.json(); if (!r.ok) throw Error(d.error || 'Request failed'); return d; });
const esc = s => String(s || '').replace(/[&<>"']/g, c => ({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;' }[c]));
function auth(mode='login', error='') { app.innerHTML = `<main class="auth"><section><div class="brand">✦ <b>Flowboard</b></div><p class="eyebrow">WORKFLOW OPERATIONS</p><h1>${mode==='login'?'Welcome back.':'Create your workspace.'}</h1><p class="muted">${mode==='login'?'Sign in to manage your workflows.':'Build and run durable workflows.'}</p>${error?`<div class="error">${esc(error)}</div>`:''}<form id="form">${mode==='register'?'<label>Full name<input name="name" required></label>':''}<label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" minlength="8" required></label><button class="primary">${mode==='login'?'Sign in':'Create account'} <span>→</span></button></form><button class="link" id="switch">${mode==='login'?'New here? Create an account':'Already have an account? Sign in'}</button></section></main>`;document.querySelector('#switch').onclick=()=>auth(mode==='login'?'register':'login');document.querySelector('#form').onsubmit=async e=>{e.preventDefault();try{state.user=(await api('/api/auth/'+mode,{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))})).user;router.navigate('/overview',{replace:true});await load()}catch(x){auth(mode,x.message)}}}
async function load(){const [w,i,t,j,n]=await Promise.all([api('/api/workflows'),api('/api/instances'),api('/api/tasks'),api('/api/jobs'),api('/api/incidents')]);Object.assign(state,{workflows:w.workflows,instances:i.instances,tasks:t.tasks,jobs:j.jobs,incidents:n.incidents});router.render();}
function render(){app.innerHTML=`<div class="shell"><aside><div class="brand">✦<b>Flowboard</b></div><p class="eyebrow">OPERATIONS</p><nav>${[['overview','Overview','⌂'],['workflows','Workflows','◇'],['instances','Instances','◌'],['tasks','Tasks','✓']].map(x=>`<button class="nav ${state.view===x[0]?'active':''}" data-path="/${x[0]}">${x[2]} ${x[1]}</button>`).join('')}</nav><div class="profile"><i>${esc(state.user.name[0])}</i><span><b>${esc(state.user.name)}</b><small>${esc(state.user.email)}</small></span><button id="logout">↗</button></div></aside><main class="content"><header><div><p class="eyebrow">${new Date().toLocaleDateString(undefined,{weekday:'long',month:'long',day:'numeric'})}</p><h1>${state.view[0].toUpperCase()+state.view.slice(1)}</h1></div><button id="refresh" class="refresh">↻</button></header>${content()}</main></div>`;document.querySelectorAll('[data-path]').forEach(b=>b.onclick=()=>router.navigate(b.dataset.path));document.querySelector('#refresh').onclick=load;document.querySelector('#logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'});state.user=null;router.navigate('/login')}}
function contentLegacy(){return '';}
function workflowCard(w){return `<section class="panel workflow-card"><div class="panel-head"><div><h2>${esc(w.name)}</h2><p class="muted">${esc(w.key)} · version ${w.version}</p></div><label class="status">${esc(w.status)}</label></div>${workflowSvg(w)}</section>`}
function content(){if(state.view==='workflows')return `<div class="workflow-grid">${state.workflows.map(workflowCard).join('')||'<div class="empty">No workflows deployed yet.</div>'}</div>`;if(state.view==='instances')return panel('Process instances',state.instances.map(i=>[i.processKey,i.businessKey||'—',i.status]));if(state.view==='tasks')return panel('Human tasks',state.tasks.map(t=>[t.name,t.assignee||'Unassigned',t.status]));if(state.view==='jobs')return panel('Jobs',state.jobs.map(j=>[`${j.type} #${j.id}`,`Retries: ${j.retries}`,j.status]));if(state.view==='incidents')return panel('Incidents',state.incidents.map(n=>[n.type,n.message,n.status]));return `<section class="stats"><div><small>WORKFLOWS</small><strong>${state.workflows.length}</strong><span>deployed definitions</span></div><div><small>RUNNING INSTANCES</small><strong>${state.instances.filter(i=>i.status==='RUNNING').length}</strong><span>active executions</span></div><div><small>OPEN TASKS</small><strong>${state.tasks.filter(t=>t.status!=='COMPLETED').length}</strong><span>awaiting action</span></div></section>${panel('Recent activity',state.instances.slice(0,5).map(i=>[i.processKey,i.businessKey||'No business key',i.status]))}`}
function workflowSvg(w){const ns=w.nodes||[],es=w.edges||[],width=Math.max(640,ns.length*150),pos=new Map(ns.map((n,i)=>[n.key,{x:70+i*(width-140)/Math.max(1,ns.length-1),y:110}])),colors={START:'#8cf0c0',END:'#ff9eaa',USER_TASK:'#83c9ff',SERVICE_TASK:'#d8b4ff',SCRIPT_TASK:'#d8b4ff',EXCLUSIVE_GATEWAY:'#ffd580',PARALLEL_GATEWAY:'#ffd580',TIMER:'#83c9ff',MESSAGE:'#83c9ff'};const lines=es.map(e=>{const a=pos.get(e.from),b=pos.get(e.to);return a&&b?`<line x1="${a.x+45}" y1="110" x2="${b.x-45}" y2="110"/><text x="${(a.x+b.x)/2}" y="101">${esc(e.condition||'')}</text>`:''}).join('');const shapes=ns.map(n=>{const p=pos.get(n.key);return `<g><rect x="${p.x-45}" y="85" width="90" height="50" rx="${n.type.includes('GATEWAY')?'25':'8'}" fill="${colors[n.type]||'#9daebe'}"/><text class="node-type" x="${p.x}" y="108">${esc(n.type.replace('_',' '))}</text><text class="node-label" x="${p.x}" y="123">${esc((n.name||n.key).slice(0,15))}</text></g>`}).join('');return `<div class="graph-wrap"><svg class="workflow-svg" viewBox="0 0 ${width} 220" role="img" aria-label="${esc(w.name)} workflow"><g class="edges">${lines}</g>${shapes}</svg></div>`}
function panel(title,rows){return `<section class="panel"><div class="panel-head"><div><h2>${title}</h2><p class="muted">Live data from SQLite.</p></div><em>${rows.length}</em></div>${rows.map(r=>`<div class="row"><span class="dot">◇</span><div><b>${esc(r[0])}</b><small>${esc(r[1])}</small></div><label class="status">${esc(r[2])}</label></div>`).join('')||'<div class="empty">Nothing to show yet.</div>'}</section>`}
router.addRoute('/',()=>router.navigate('/overview',{replace:true})).addRoute('/overview',()=>{state.view='overview';render()}).addRoute('/workflows',()=>{state.view='workflows';render()}).addRoute('/instances',()=>{state.view='instances';render()}).addRoute('/tasks',()=>{state.view='tasks';render()}).addRoute('/login',()=>auth()).addRoute('*',()=>router.navigate('/overview',{replace:true}));api('/api/auth').then(d=>d.user?(state.user=d.user,load()):router.navigate('/login',{replace:true})).catch(()=>router.navigate('/login',{replace:true}));
